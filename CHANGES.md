# سجل التغييرات — ربط المشروع بـ Supabase

تحويل المشروع من نموذج أولي ببيانات ثابتة إلى نظام يحفظ بياناته فعلياً.

---

## 1. ملفات جديدة

| الملف | الدور |
| --- | --- |
| `supabase/schema.sql` | مخطط قاعدة البيانات كاملاً: الجداول، الأنواع، الفهارس، سياسات RLS، ومُشغّل ربط الحساب بملفه |
| `js/config.js` | رابط المشروع والمفتاح المنشور (publishable) — عام وآمن |
| `js/supabase.js` | عميل Supabase وطبقة الوصول للبيانات والمصادقة (ES module من CDN) |
| `CHANGES.md` | هذا الملف |

## 2. مخطط قاعدة البيانات

ستة جداول، وكل قيد فيها له سبب:

| الجدول | الأعمدة | ملاحظة |
| --- | --- | --- |
| `profiles` | id → auth.users، الاسم، **الدور**، القسم | مصدر الدور الوحيد. الخادم يملكه، فلا يستطيع مستخدم ترقية نفسه |
| `guardians` | الاسم، الجوال، البريد، user_id | `user_id` اختياري: يُسجَّل وليّ الأمر قبل أن يكون له حساب |
| `staff` | الاسم، التخصص، القسم، **رقم الهوية الوظيفية** | رقم الهوية `unique` |
| `students` | الاسم، الصف، القسم، وليّ الأمر، **حالة الرسوم** | مربوط بـ `guardians` |
| `attendance` | الطالب، التاريخ، الحالة | `unique(student_id, date)` — إعادة الرصد تستبدل ولا تُكرّر |
| `grades` | الطالب، المادة، الدرجة، الفصل | `unique(student_id, subject, term)`، والدرجة مقيّدة بين 0 و100 |

القسم (بنين/بنات) وحالة الحضور وحالة الرسوم والدور: **أنواع `enum`** لا نصوص حرة، فلا يدخل الجدول قيمة لا معنى لها.

### سياسات الأمان (RLS)

المفتاح في المتصفح لا يتجاوزها — **قاعدة البيانات** هي التي تمنع، لا الواجهة:

| الدور | ما يراه |
| --- | --- |
| الإدارة | كل شيء |
| المعلم | طلاب قسمه وحده، وحضورهم ودرجاتهم |
| وليّ الأمر | أبناؤه وحدهم |

استُخدمت دوال `SECURITY DEFINER` لقراءة الدور، لأن سياسة على `profiles` تستعلم من `profiles` تُسبّب تكراراً لا نهائياً — الدالة تكسر الحلقة.

## 3. تسجيل دخول حقيقي

**قبل:** اختيار الدور من ثلاثة أزرار ثم `setTimeout(700)` يفتح اللوحة. أي أحد يدخل أي لوحة.

**بعد:** `signInWithPassword` فعلي مقابل Supabase Auth.

- اللوحة التي تُفتح تأتي من `profiles.role` **لا من الزر الذي ضغطه المستخدم**. أزرار الأدوار بقيت كما هي في التصميم لكنها صارت مجرد تلميح.
- قسم المعلم (`campus`) يأتي من الملف الشخصي أيضاً.
- «تسجيل الخروج» في اللوحات الثلاث صار ينهي الجلسة فعلياً (`signOut`) بدل الانتقال لصفحة الدخول فقط — فلا يستأنف الزائر التالي جلسة سابقة من الرمز المحفوظ.
- إعادة تحميل الصفحة تُبقيك داخل حسابك، وتفتح لوحة دورك.
- رسائل الخطأ مترجمة للحالات التي يستطيع المستخدم التصرف حيالها: بيانات خاطئة، بريد غير مؤكَّد، محاولات كثيرة، انقطاع الشبكة.

**تم اختباره فعلياً:** بيانات خاطئة أُرسلت للخادم وعادت برفض صحيح ورسالة عربية، والمستخدم بقي في صفحة الدخول.

## 4. الحفظ الفعلي

نافذتا «إضافة طالب» و«إضافة معلم» صارتا تكتبان في قاعدة البيانات:

- النافذة **لا تُغلق حتى يصل الصف فعلاً** — فالفشل لا يبدو كنجاح أبداً.
- بعد النجاح تُعاد قراءة القائمة **من الخادم** لا من المدخلات — فما تراه هو ما حُفظ حقاً.
- وليّ الأمر المكتوب في نموذج الطالب يُنشأ أولاً ثم يُربط الطالب به. ويُعاد استخدام وليّ أمر بالاسم نفسه بدل تكراره، حتى لا تتشعّب صفوف الإخوة.
- رقم الهوية الوظيفية يُولَّد تلقائياً لأن التصميم لا يحوي حقلاً له، فيبقى قيد الفرادة ذا معنى.

## 5. مبدأ: التصميم لا ينكسر

الشاشات صُمّمت على محتوى ثابت. الربط يُبقي ذلك المحتوى **حالة سكون** ويضع البيانات الحقيقية فوقه:

- إن لم يُطبَّق المخطط بعد، أو كان الجدول فارغاً، تُعرض الشاشة كما صُمّمت تماماً.
- الأرقام الحيّة (`data-live`) لا تستبدل قيمة التصميم إلا بعد وصول رقم حقيقي — فلا تومض الشاشة بأصفار.
- `supabase.js` وحدة ES تعمل بعد السكربتات العادية، فـ `app.js` ينتظر حدث `nasr:ready` بدل افتراض وجودها.
- لا دالة في طبقة البيانات ترمي استثناءً أو تفتح `alert` — كلها تُعيد `{ data, error, hint }` والشاشة تقرر.

## 6. خلل اكتُشف أثناء العمل

فحص الجاهزية كان يستخدم `head: true`، و**PostgREST يردّ على طلب HEAD لجدول غير موجود بـ 204 بلا خطأ** — لأن HEAD بلا جسم يحمل الخطأ. النتيجة: كل قاعدة بيانات تبدو جاهزة، وكانت الأرقام الحقيقية ستمسح محتوى التصميم بأصفار.

استُبدل بـ `select().limit(1)` الذي يُبلّغ عن الغياب بشكل صحيح (`PGRST205`). تحققت من الإصلاح: `dbReady` صار `false` بحق، ومؤشرات لوحة الإدارة أبقت قيم التصميم.

## 7. اختبار الانحدار

بعد كل ما سبق، أعيد فحص المشروع كاملاً:

- **68 تركيبة** (لغتان × 3 أدوار × كل الأقسام × قسمَي المعلمين): لا قسم فارغ، وزر تنقل نشط واحد في كل حالة
- صفر مفاتيح ترجمة ناقصة · صفر إجراءات غير معرّفة · صفر أخطاء console
- التنقل ومسارات التنقل وشات القسمين تعمل كما كانت

---

## ما تبقّى

### مطلوب منك (دقيقة واحدة)

شغّل `supabase/schema.sql` في **Supabase → SQL Editor → Run**.

المفتاح المنشور يقرأ ويكتب الصفوف لكنه لا يُنشئ الجداول، والمفتاح السرّي لا يُنشئها أيضاً — إنشاء الجداول يحتاج Personal Access Token أو كلمة مرور قاعدة البيانات. الملف آمن لإعادة التشغيل.

بعد تشغيله يعمل كل ما سبق مباشرة بلا أي تعديل في الكود.

### ثم حسابات الدخول

تأكيد البريد مفعّل في المشروع (`mailer_autoconfirm: false`)، فأي حساب جديد لن يدخل قبل تأكيد بريده. للتطوير: أطفئ **Confirm email** من Authentication → Sign In / Providers → Email، أو أنشئ الحسابات يدوياً من Authentication → Users مع **Auto Confirm**.

الدور يُقرأ من `profiles`، ويُملأ تلقائياً من بيانات التسجيل عبر مُشغّل `on_auth_user_created`. لإنشاء حساب إدارة من لوحة Supabase، أضف في **User Metadata**:

```json
{ "full_name": "عبدالله السالم", "role": "admin" }
```

وللمعلم: `{ "full_name": "هند ناصر", "role": "staff", "campus": "girls" }`

### لم يُربط بعد

الأقسام التي بقيت على محتوى التصميم لأنها تحتاج قرارات منتج لا مجرد ربط: التقويم، التقارير، الرسوم والفواتير، الرسائل، أسئلة أولياء الأمور، المهام، والمحادثات. طبقة البيانات جاهزة لها (`listAttendance`، `markAttendance`، `listGrades`، `saveGrade` مكتوبة ومختبَرة الشكل)، لكن ربط جداول الحضور والدرجات بالواجهة يحتاج تحديد أي فصل وأي فترة تُعرض افتراضياً.

### الرفع على GitHub

`gh` غير مثبّت على الجهاز ولا يوجد remote مضبوط، فالرفع متعذّر من هنا. أُنشئ مستودع محلي وسُجّل commit كامل — يبقى أمر واحد للرفع بعد إنشاء المستودع على GitHub.

---

## 8. إصلاح: تسجيل الدخول كان يفشل عند فتح الملف مباشرة

**العرض:** «تعذّر الوصول إلى الخادم. تحقّق من اتصالك.» عند محاولة الدخول.

**السبب:** `js/supabase.js` كان وحدة ES (`<script type="module">`)، **والمتصفح يرفض
الوحدات عبر `file://`** بسبب CORS. والمشروع يُفتح من القرص مباشرة بقدر ما يُقدَّم
عبر خادم. النتيجة: طبقة البيانات لا تُحمَّل إطلاقاً، `window.NasrAuth` غير معرّف،
فتظهر رسالة الشبكة — وهي رسالة مضلِّلة توجّه المستخدم لفحص اتصاله بينما الاتصال سليم.

**الإصلاح:**

- تحوّل `js/supabase.js` إلى **سكربت عادي** داخل IIFE، ويستهلك بناء **UMD** من
  supabase-js الذي يعرّف `window.supabase`. يعمل الآن من `file://` ومن `http://` معاً.
- إن تعذّر تحميل المكتبة من الـ CDN، تُقال الحقيقة: «لم تُحمَّل مكتبة الاتصال
  بقاعدة البيانات» (`authNoLib`) بدل اتهام اتصال المستخدم.

**تم اختباره فعلياً في المتصفح بعد الإصلاح:**

| الخطوة | النتيجة |
| --- | --- |
| تحميل المكتبة | `window.supabase` معرّف، `NasrAuth`/`NasrApi` جاهزان |
| دخول `admin@nasr.edu.sa` | نجح — وصل إلى `#/admin` |
| مصدر الدور | `profiles.role` من الخادم لا من زر الواجهة |
| إضافة طالب من النافذة | حُفظ، ووليّ الأمر أُنشئ وارتبط به |
| إضافة معلم | حُفظ برقم وظيفي مولَّد |
| **إعادة تحميل الصفحة** | الجلسة باقية، والصفوف باقية، والمؤشرات تعرض 1 و1 |

---

## 9. حارس المسارات (route guard)

**الثغرة:** أي زائر يكتب `#/admin` أو `#/staff` أو `#/parent` في العنوان يرى هيكل
اللوحة. لا تتسرّب بيانات — RLS يمنع كل قراءة — لكن الشاشة تُعرض.

**المعضلة الحقيقية كانت التوقيت.** الجلسة لا تُعرف إلا بعد تحميل supabase-js
وانتهاء `getSession()`، وهذا بعد أول رسم للصفحة بوقت. فحارس يفترض «غير مسجّل»
حتى ذلك الحين يقذف المستخدم المسجّل إلى صفحة الدخول ثم يعيده عند كل تحديث؛ وحارس
يفترض «مسجّل» يعرض هيكل اللوحة لغريب لبضع مئات من الأجزاء من الثانية.

**الحل: للمصادقة ثلاث حالات لا اثنتان.**

```
pending → شاشة «جارٍ التحقق من الجلسة» — لا لوحة ولا صفحة دخول
in      → اللوحة المطابقة للدور
out     → صفحة الدخول
```

`allowedScreen()` هو الموضع الوحيد الذي يقرر أي شاشة تُعرض، وتمرّ به كل نقاط
التنقل: `goScreen`، و`applyPath` (زر رجوع المتصفح)، و`restore` (الإقلاع).

- الصفحة العامة وصفحة الدخول **لا تمرّان بالحارس إطلاقاً** — مفتوحتان دائماً.
- الدور يأتي من `profiles.role`. معلم يطلب `#/admin` يُحوَّل إلى لوحته هو.
- جلسة بلا صف في `profiles` تُعامَل كغير مسجّلة، لا كمسجّلة بلا دور.
- **إن تعذّر تحميل المكتبة، يُعامَل الزائر كغير مسجّل.** الافتراض المعاكس يسلّم
  اللوحات لكل من فشل طلبه إلى الـ CDN.
- مهلة 8 ثوانٍ تمنع شاشة التحقق من أن تصبح طريقاً مسدوداً إن لم تصل الحالة أبداً.

`state.wanted` و`state.wantedSection` يحفظان الوجهة المطلوبة أثناء التحقق، فيصل
المستخدم إلى ما قصده بعد وصول الجواب.

### انحدار أمسكته أثناء التنفيذ

أول نسخة كانت تفقد القسم في الرابط العميق: إعادة تحميل `#/admin/finance` تصل إلى
`admin/overview`، لأن القسم كان يُصفَّر بينما الشاشة محتجزة عند `checking`.
أُضيف `wantedSection` ليُستعاد القسم لحظة الإفراج عن اللوحة. تحققت: العنوان يعود
إلى `#/admin/finance` بالضبط.

### الاختبار في المتصفح

| الحالة | النتيجة |
| --- | --- |
| فتح `#/admin` بلا دخول | ✅ حُوِّل إلى `#/login` |
| **هل ظهر هيكل اللوحة للحظة؟** | ✅ **لا** — رصدت الصفحة من `iframe` طوال إقلاعها: `checking` ثم `login`، ولم تظهر اللوحة في أي عيّنة |
| `#/staff` و`#/parent` بلا دخول | ✅ كلاهما إلى صفحة الدخول |
| الصفحة العامة | ✅ مفتوحة كما هي |
| صفحة الدخول | ✅ مفتوحة كما هي |
| دخول كـ admin | ✅ وصل للوحة الإدارة والتنقل طبيعي |
| إعادة تحميل على `#/admin/finance` | ✅ عاد إلى finance بلا ارتداد |
| عدم تطابق الدور | ✅ طلب `staff` وهو admin → أُعيد إلى admin |
| تسجيل الخروج | ✅ الجلسة انتهت واللوحات أُغلقت من جديد |
| انحدار الواجهة | ✅ 68 تركيبة بلا قسم فارغ، وصفر مفاتيح ناقصة، وصفر أخطاء |

---

# ⚠️ الوضع الحالي: معاينة عامة ببيانات وهمية

**النسخة المنشورة على GitHub Pages لا تتصل بقاعدة البيانات إطلاقاً.**

هذا مقصود: الرابط يُشارَك مع خبير تقني ومع المدرسة للمعاينة، فيجب أن يُتصفَّح
بحرية كاملة بلا تسجيل دخول، وبلا أي بيانات مدرسة حقيقية على صفحة عامة.

## كيف يعمل المفتاح

كل شيء يتحكّم به سطر واحد في `js/config.js`:

```js
mode: 'demo'   // 'demo' = بيانات وهمية بلا اتصال · 'live' = قاعدة البيانات الحقيقية
```

| | `demo` (الآن) | `live` |
| --- | --- | --- |
| الاتصال بـ Supabase | لا شيء إطلاقاً | كامل |
| تسجيل الدخول | اختيار الدور فقط، كما كان النموذج الأولي | Supabase Auth حقيقي |
| حارس المسارات | معطّل — كل اللوحات مفتوحة | مفعّل — جلسة ودور مطابق |
| البيانات | ثابتة داخل `index.html` | من قاعدة البيانات |
| نوافذ الإضافة | تُغلق بلا حفظ | تكتب في القاعدة |

## ما لم يُحذف

**كود الاتصال الحقيقي كامل وسليم، لم يُمَس منه شيء:**

- `js/supabase.js` — العميل وطبقة البيانات والمصادقة
- `supabase/schema.sql` — المخطط وسياسات RLS
- `js/app.js` — تسجيل الدخول الفعلي وحارس المسارات

كلها موجودة خلف بوابات `if (window.NasrDemo)`. لا شيء عُلّق ولا نُسخ ولا فُرّغ.

## في وضع المعاينة لا يُرسَل أي طلب خارجي إطلاقاً

مكتبة supabase-js كانت تُحمَّل من CDN عبر وسم `<script>` ثابت في `index.html`.
نُقل تحميلها إلى داخل `js/supabase.js` ولا يحدث إلا في وضع `live`. فالنسخة
المنشورة لا تطلب شيئاً من أي جهة خارجية — لا Supabase ولا jsDelivr.

**تحققت من ذلك برصد شبكة المتصفح:** كل الطلبات إلى الخادم المحلي فقط.

## إعادة التفعيل لاحقاً — خطوة واحدة

غيّر `mode` إلى `'live'` في `js/config.js`. لا شيء غير ذلك.

ثم تحقّق قبل التسليم من:

1. أن `supabase/schema.sql` مُطبَّق على المشروع المستهدف.
2. أن حسابات الدخول منشأة وأدوارها مضبوطة في `profiles`.
3. أن الرابط والمفتاح في `config.js` يخصّان مشروع المدرسة لا مشروع التجربة.
4. مراجعة سياسات RLS قبل نشر نسخة حيّة على صفحة عامة — الحماية كلها في قاعدة
   البيانات لا في الواجهة.

## الاختبار

| الحالة | النتيجة |
| --- | --- |
| طلبات خارجية في وضع المعاينة | ✅ **صفر** — رُصدت الشبكة، كلها محلية |
| `NasrApi` / `NasrAuth` | ✅ غائبان تماماً، لا عميل يُنشأ |
| فتح `#/admin` بلا دخول | ✅ يفتح مباشرة ببيانات كاملة |
| `#/staff` و`#/parent` | ✅ كلاهما يفتح مباشرة |
| تصفّح كل الأقسام | ✅ 68 تركيبة (لغتان × 3 أدوار × كل الأقسام × قسمَي المعلمين) بلا قسم فارغ |
| مؤشرات لوحة الإدارة | ✅ 1,248 و86 — أرقام التصميم كما هي |
| تسجيل الدخول التجريبي | ✅ اختيار «معلم» ثم دخول → لوحة المعلم |
| نوافذ الإضافة | ✅ تفتح وتُغلق بلا أي كتابة |
| **العودة إلى `live`** | ✅ جُرّبت فعلياً: المكتبة تُحمَّل، والحارس يعمل، والدخول الحقيقي نجح وقرأ الصفوف من القاعدة — ثم أُعيد المفتاح إلى `demo` |
