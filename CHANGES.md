# سجل التغييرات — ربط المشروع بـ Supabase

تحويل المشروع من نموذج أولي ببيانات ثابتة إلى نظام يحفظ بياناته فعلياً.

---

## 1. ملفات جديدة

| الملف | الدور |
| --- | --- |
| `supabase/schema.sql` | مخطط قاعدة البيانات كاملاً: الجداول، الأنواع، الفهارس، سياسات RLS، ومُشغّل ربط الحساب بملفه |
| `js/config.js` | رابط المشروع والمفتاح المنشور (publishable) — عام وآمن |
| `js/supabase.js` | عميل Supabase وطبقة الوصول للبيانات والمصادقة (ES module من CDN) |
| `CHANGES.md` | هذا الملف |

## 2. مخطط قاعدة البيانات

ستة جداول، وكل قيد فيها له سبب:

| الجدول | الأعمدة | ملاحظة |
| --- | --- | --- |
| `profiles` | id → auth.users، الاسم، **الدور**، القسم | مصدر الدور الوحيد. الخادم يملكه، فلا يستطيع مستخدم ترقية نفسه |
| `guardians` | الاسم، الجوال، البريد، user_id | `user_id` اختياري: يُسجَّل وليّ الأمر قبل أن يكون له حساب |
| `staff` | الاسم، التخصص، القسم، **رقم الهوية الوظيفية** | رقم الهوية `unique` |
| `students` | الاسم، الصف، القسم، وليّ الأمر، **حالة الرسوم** | مربوط بـ `guardians` |
| `attendance` | الطالب، التاريخ، الحالة | `unique(student_id, date)` — إعادة الرصد تستبدل ولا تُكرّر |
| `grades` | الطالب، المادة، الدرجة، الفصل | `unique(student_id, subject, term)`، والدرجة مقيّدة بين 0 و100 |

القسم (بنين/بنات) وحالة الحضور وحالة الرسوم والدور: **أنواع `enum`** لا نصوص حرة، فلا يدخل الجدول قيمة لا معنى لها.

### سياسات الأمان (RLS)

المفتاح في المتصفح لا يتجاوزها — **قاعدة البيانات** هي التي تمنع، لا الواجهة:

| الدور | ما يراه |
| --- | --- |
| الإدارة | كل شيء |
| المعلم | طلاب قسمه وحده، وحضورهم ودرجاتهم |
| وليّ الأمر | أبناؤه وحدهم |

استُخدمت دوال `SECURITY DEFINER` لقراءة الدور، لأن سياسة على `profiles` تستعلم من `profiles` تُسبّب تكراراً لا نهائياً — الدالة تكسر الحلقة.

## 3. تسجيل دخول حقيقي

**قبل:** اختيار الدور من ثلاثة أزرار ثم `setTimeout(700)` يفتح اللوحة. أي أحد يدخل أي لوحة.

**بعد:** `signInWithPassword` فعلي مقابل Supabase Auth.

- اللوحة التي تُفتح تأتي من `profiles.role` **لا من الزر الذي ضغطه المستخدم**. أزرار الأدوار بقيت كما هي في التصميم لكنها صارت مجرد تلميح.
- قسم المعلم (`campus`) يأتي من الملف الشخصي أيضاً.
- «تسجيل الخروج» في اللوحات الثلاث صار ينهي الجلسة فعلياً (`signOut`) بدل الانتقال لصفحة الدخول فقط — فلا يستأنف الزائر التالي جلسة سابقة من الرمز المحفوظ.
- إعادة تحميل الصفحة تُبقيك داخل حسابك، وتفتح لوحة دورك.
- رسائل الخطأ مترجمة للحالات التي يستطيع المستخدم التصرف حيالها: بيانات خاطئة، بريد غير مؤكَّد، محاولات كثيرة، انقطاع الشبكة.

**تم اختباره فعلياً:** بيانات خاطئة أُرسلت للخادم وعادت برفض صحيح ورسالة عربية، والمستخدم بقي في صفحة الدخول.

## 4. الحفظ الفعلي

نافذتا «إضافة طالب» و«إضافة معلم» صارتا تكتبان في قاعدة البيانات:

- النافذة **لا تُغلق حتى يصل الصف فعلاً** — فالفشل لا يبدو كنجاح أبداً.
- بعد النجاح تُعاد قراءة القائمة **من الخادم** لا من المدخلات — فما تراه هو ما حُفظ حقاً.
- وليّ الأمر المكتوب في نموذج الطالب يُنشأ أولاً ثم يُربط الطالب به. ويُعاد استخدام وليّ أمر بالاسم نفسه بدل تكراره، حتى لا تتشعّب صفوف الإخوة.
- رقم الهوية الوظيفية يُولَّد تلقائياً لأن التصميم لا يحوي حقلاً له، فيبقى قيد الفرادة ذا معنى.

## 5. مبدأ: التصميم لا ينكسر

الشاشات صُمّمت على محتوى ثابت. الربط يُبقي ذلك المحتوى **حالة سكون** ويضع البيانات الحقيقية فوقه:

- إن لم يُطبَّق المخطط بعد، أو كان الجدول فارغاً، تُعرض الشاشة كما صُمّمت تماماً.
- الأرقام الحيّة (`data-live`) لا تستبدل قيمة التصميم إلا بعد وصول رقم حقيقي — فلا تومض الشاشة بأصفار.
- `supabase.js` وحدة ES تعمل بعد السكربتات العادية، فـ `app.js` ينتظر حدث `nasr:ready` بدل افتراض وجودها.
- لا دالة في طبقة البيانات ترمي استثناءً أو تفتح `alert` — كلها تُعيد `{ data, error, hint }` والشاشة تقرر.

## 6. خلل اكتُشف أثناء العمل

فحص الجاهزية كان يستخدم `head: true`، و**PostgREST يردّ على طلب HEAD لجدول غير موجود بـ 204 بلا خطأ** — لأن HEAD بلا جسم يحمل الخطأ. النتيجة: كل قاعدة بيانات تبدو جاهزة، وكانت الأرقام الحقيقية ستمسح محتوى التصميم بأصفار.

استُبدل بـ `select().limit(1)` الذي يُبلّغ عن الغياب بشكل صحيح (`PGRST205`). تحققت من الإصلاح: `dbReady` صار `false` بحق، ومؤشرات لوحة الإدارة أبقت قيم التصميم.

## 7. اختبار الانحدار

بعد كل ما سبق، أعيد فحص المشروع كاملاً:

- **68 تركيبة** (لغتان × 3 أدوار × كل الأقسام × قسمَي المعلمين): لا قسم فارغ، وزر تنقل نشط واحد في كل حالة
- صفر مفاتيح ترجمة ناقصة · صفر إجراءات غير معرّفة · صفر أخطاء console
- التنقل ومسارات التنقل وشات القسمين تعمل كما كانت

---

## ما تبقّى

### مطلوب منك (دقيقة واحدة)

شغّل `supabase/schema.sql` في **Supabase → SQL Editor → Run**.

المفتاح المنشور يقرأ ويكتب الصفوف لكنه لا يُنشئ الجداول، والمفتاح السرّي لا يُنشئها أيضاً — إنشاء الجداول يحتاج Personal Access Token أو كلمة مرور قاعدة البيانات. الملف آمن لإعادة التشغيل.

بعد تشغيله يعمل كل ما سبق مباشرة بلا أي تعديل في الكود.

### ثم حسابات الدخول

تأكيد البريد مفعّل في المشروع (`mailer_autoconfirm: false`)، فأي حساب جديد لن يدخل قبل تأكيد بريده. للتطوير: أطفئ **Confirm email** من Authentication → Sign In / Providers → Email، أو أنشئ الحسابات يدوياً من Authentication → Users مع **Auto Confirm**.

الدور يُقرأ من `profiles`، ويُملأ تلقائياً من بيانات التسجيل عبر مُشغّل `on_auth_user_created`. لإنشاء حساب إدارة من لوحة Supabase، أضف في **User Metadata**:

```json
{ "full_name": "عبدالله السالم", "role": "admin" }
```

وللمعلم: `{ "full_name": "هند ناصر", "role": "staff", "campus": "girls" }`

### لم يُربط بعد

الأقسام التي بقيت على محتوى التصميم لأنها تحتاج قرارات منتج لا مجرد ربط: التقويم، التقارير، الرسوم والفواتير، الرسائل، أسئلة أولياء الأمور، المهام، والمحادثات. طبقة البيانات جاهزة لها (`listAttendance`، `markAttendance`، `listGrades`، `saveGrade` مكتوبة ومختبَرة الشكل)، لكن ربط جداول الحضور والدرجات بالواجهة يحتاج تحديد أي فصل وأي فترة تُعرض افتراضياً.

### الرفع على GitHub

`gh` غير مثبّت على الجهاز ولا يوجد remote مضبوط، فالرفع متعذّر من هنا. أُنشئ مستودع محلي وسُجّل commit كامل — يبقى أمر واحد للرفع بعد إنشاء المستودع على GitHub.
